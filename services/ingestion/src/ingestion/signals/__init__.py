"""Agent signals — automated, descriptive desk-notes posted into stock feeds.

Detection is pure (compare today vs yesterday); publishing is deduped via the signal_events ledger.
Facts + a plain "what it means", never advice. See docs/specs/agent-signals.md.
"""
